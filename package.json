{
	"name": "jammers-pwa",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.html",
	"scripts": {
		"build": "workbox injectManifest",
		"local-server": "json-server --watch api/db.json --port 8081",
		"serve-app": "live-server --cors --open=app --port=8080 --host=0.0.0.0",
		"serve-pwa":"live-server --cors --open=pwa --port=8080 --host=0.0.0.0",
		"start":"npm run local-server && npm run serve-app"
	},
	"author": "Snickdx",
	"license": "ISC",
	"devDependencies": {
		"json-server": "^0.12.1",
		"live-server": "^1.2.1",
		"workbox-cli": "^3.3.0"
	},
	"dependencies": {
		"path": "^0.12.7"
	}
}
