{
	"name": "jammers-pwa",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.html",
	"scripts": {
		"build": "workbox injectManifest",
		"local-server": "json-server --watch api/db.json --port 8081",
		"serve-app": "cd app & live-server --cors --port=8080 --host=0.0.0.0",
		"serve-pwa": "cd pwa & live-server --cors --port=8080 --host=0.0.0.0",
		"start": "npm run local-server | npm run serve-app",
		"start-pwa": "npm run local-server | npm run serve-pwa"
	},
	"author": "Snickdx",
	"license": "ISC",
	"devDependencies": {
		"json-server": "^0.12.1",
		"live-server": "^1.2.1",
		"workbox-cli": "^3.3.0"
	},
	"dependencies": {
		"path": "^0.12.7"
	}
}
